function sumaNumeros(NE1: number, NE2: number): number {
    const suma = NE1 + NE2;
    return suma;
  }
  
  const numero1 = 5;
  const numero2 = 3;
  const resultado = sumaNumeros(numero1, numero2);
  
  console.log(`La suma de ${numero1} y ${numero2} es ${resultado}`);

//Hola mundo
